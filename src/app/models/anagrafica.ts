export interface Anagrafica {
    id: number,
    nome : string,
    cognome: string,
    stato: string,
    citta: string,
    provincia: string,
    cap: string,
    via : string,
    predefinito : boolean
}
